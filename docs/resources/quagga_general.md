---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "opnsense_quagga_general Resource - opnsense"
subcategory: ""
description: |-
  Manages FRR (Quagga) general service settings on OPNsense. This is a singleton resource — there is one FRR configuration per appliance; terraform destroy only removes it from state and does not delete the appliance config. Requires the os-frr plugin.
---

# opnsense_quagga_general (Resource)

Manages FRR (Quagga) general service settings on OPNsense. This is a singleton resource — there is one FRR configuration per appliance; `terraform destroy` only removes it from state and does not delete the appliance config. Requires the `os-frr` plugin.

## Example Usage

```terraform
resource "opnsense_quagga_general" "this" {
  enabled       = true
  profile       = "datacenter"
  enable_syslog = true
  syslog_level  = "notifications"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `enable_carp` (Boolean) Enable CARP failover integration. Defaults to `false`.
- `enable_snmp` (Boolean) Enable SNMP (AgentX). Defaults to `false`.
- `enable_syslog` (Boolean) Enable syslog logging. Defaults to `true`.
- `enabled` (Boolean) Enable the FRR routing service. Defaults to `false`.
- `firewall_rules` (Boolean) Automatically generate firewall rules for routing protocols. Defaults to `true`.
- `profile` (String) Routing profile: `traditional` or `datacenter`. Defaults to `traditional`.
- `syslog_level` (String) Syslog verbosity level. One of `critical`, `emergencies`, `errors`, `alerts`, `warnings`, `notifications`, `informational`, `debugging`.

### Read-Only

- `id` (String) Synthetic identifier for this singleton (always `general`).
