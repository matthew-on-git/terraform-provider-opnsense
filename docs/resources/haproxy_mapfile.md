---
page_title: "opnsense_haproxy_mapfile Resource - opnsense"
subcategory: "HAProxy"
description: |-
  Manages an HAProxy map file on OPNsense.
---

# opnsense_haproxy_mapfile (Resource)

Manages an HAProxy map file on OPNsense. Map files store key/value routing tables such as domain-to-backend mappings for `map_use_backend` actions. Requires the `os-haproxy` plugin.

OPNsense stores mapfile content as one object. This provider normalizes trailing whitespace in `content` to avoid drift from heredoc trailing newlines. Keep one mapping per line, with values matching the HAProxy backend names that HAProxy resolves at runtime.

## Example Usage

```terraform
# Domain map used by a map_use_backend HAProxy action.
resource "opnsense_haproxy_mapfile" "domain_map" {
  name        = "domain-map"
  description = "Host to backend routing map"
  type        = "dom"
  content     = <<-EOT
    grafana.example.com grafana-backend
    argocd.example.com argocd-backend
    tipsyhive.example.com tipsyhive-backend
    www.tipsyhive.example.com tipsyhive-backend
  EOT
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `content` (String) Map file content as newline-separated key/value pairs, for example one `host backend-name` mapping per line.
- `name` (String) Name of the HAProxy map file.

### Optional

- `description` (String) Description of the HAProxy map file.
- `type` (String) Map match type. Supported values: `beg`, `dom`, `end`, `int`, `ip`, `reg`, `str`, `sub`. Defaults to `dom` for domain maps.

### Read-Only

- `id` (String) UUID of the HAProxy map file in OPNsense.

## Import

Import is supported using the map file UUID from OPNsense.

```shell
# Import an existing HAProxy map file by its UUID
terraform import opnsense_haproxy_mapfile.example 12345678-1234-1234-1234-123456789012
```
