---
page_title: "opnsense_unbound_host_override Data Source - opnsense"
subcategory: "Unbound"
description: |-
  Look up an existing Unbound host override on OPNsense.
---

# opnsense_unbound_host_override (Data Source)

Look up an existing Unbound host override on OPNsense by UUID without importing it into Terraform management.

## Example Usage

```terraform
# Look up an existing Unbound host override by UUID
data "opnsense_unbound_host_override" "example" {
  id = "12345678-1234-1234-1234-123456789012"
}

output "unbound_host_override_id" {
  value = data.opnsense_unbound_host_override.example.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `id` (String) UUID to look up.

### Read-Only

- `description` (String) Description of the host override.
- `domain` (String) Domain of the override.
- `enabled` (Boolean) Whether this host override is enabled.
- `hostname` (String) Hostname of the override.
- `rr` (String) DNS record type.
- `server` (String) IP address or value for the DNS record.
