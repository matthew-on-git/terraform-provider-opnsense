---
page_title: "opnsense_quagga_bgp_aspath Data Source - opnsense"
subcategory: "Quagga / FRR"
description: |-
  Look up an existing BGP AS path rule on OPNsense.
---

# opnsense_quagga_bgp_aspath (Data Source)

Look up an existing BGP AS path rule on OPNsense. Use this data source to read attributes by UUID without importing the object into Terraform management.

## Example Usage

```terraform
# Look up an existing BGP AS path rule by UUID
data "opnsense_quagga_bgp_aspath" "example" {
  id = "12345678-1234-1234-1234-123456789012"
}

output "quagga_bgp_aspath_id" {
  value = data.opnsense_quagga_bgp_aspath.example.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `id` (String) UUID to look up.

### Read-Only

- `action` (String) AS path rule action.
- `as_pattern` (String) AS path match pattern.
- `description` (String) Description of the AS path rule.
- `enabled` (Boolean) Whether this AS path rule is enabled.
- `number` (Number) AS path rule number.
