---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: "Dnsmasq"
description: |-
  Manages a Dnsmasq DHCP range on OPNsense.
---

# {{.Name}} ({{.Type}})

Manages a Dnsmasq DHCP range on OPNsense. DHCP ranges define address pools and related DHCP behavior for Dnsmasq.

## Example Usage

{{tffile "examples/resources/opnsense_dnsmasq_range/resource.tf"}}

{{ .SchemaMarkdown | trimspace }}

## Import

Import is supported using the Dnsmasq DHCP range UUID from OPNsense.

{{codefile "shell" "examples/resources/opnsense_dnsmasq_range/import.sh"}}
