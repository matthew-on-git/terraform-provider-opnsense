---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: "Firewall"
description: |-
  Manages a firewall filter rule on OPNsense.
---

# {{.Name}} ({{.Type}})

Manages a firewall filter rule on OPNsense. Filter rules control traffic flow through the firewall.

## Example Usage

{{tffile "examples/resources/opnsense_firewall_filter_rule/resource.tf"}}

{{ .SchemaMarkdown | trimspace }}

## Import

Import is supported using the rule UUID from OPNsense.

{{codefile "shell" "examples/resources/opnsense_firewall_filter_rule/import.sh"}}
