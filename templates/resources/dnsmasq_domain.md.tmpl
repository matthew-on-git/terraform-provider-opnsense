---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: "Dnsmasq"
description: |-
  Manages a Dnsmasq domain override on OPNsense.
---

# {{.Name}} ({{.Type}})

Manages a Dnsmasq domain override on OPNsense. Domain overrides forward matching domain queries to a configured upstream address.

## Example Usage

{{tffile "examples/resources/opnsense_dnsmasq_domain/resource.tf"}}

{{ .SchemaMarkdown | trimspace }}

## Import

Import is supported using the Dnsmasq domain override UUID from OPNsense.

{{codefile "shell" "examples/resources/opnsense_dnsmasq_domain/import.sh"}}
