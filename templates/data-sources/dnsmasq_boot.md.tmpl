---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: "Dnsmasq"
description: |-
  Look up an existing Dnsmasq DHCP boot entry on OPNsense.
---

# {{.Name}} ({{.Type}})

Look up an existing Dnsmasq DHCP boot entry on OPNsense by UUID without importing it into Terraform management.

## Example Usage

{{tffile "examples/data-sources/opnsense_dnsmasq_boot/data-source.tf"}}

{{ .SchemaMarkdown | trimspace }}
