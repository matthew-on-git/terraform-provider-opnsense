---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: "HAProxy"
description: |-
  Look up an existing HAProxy server on OPNsense.
---

# {{.Name}} ({{.Type}})

Look up an existing HAProxy server on OPNsense. Use this data source to read attributes by UUID without importing the object into Terraform management.

## Example Usage

{{tffile "examples/data-sources/opnsense_haproxy_server/data-source.tf"}}

{{ .SchemaMarkdown | trimspace }}
