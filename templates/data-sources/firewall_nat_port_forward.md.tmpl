---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: "Firewall"
description: |-
  Look up an existing firewall NAT port forward rule on OPNsense.
---

# {{.Name}} ({{.Type}})

Look up an existing firewall NAT port forward rule on OPNsense. Use this data source to read attributes by UUID without importing the object into Terraform management.

## Example Usage

{{tffile "examples/data-sources/opnsense_firewall_nat_port_forward/data-source.tf"}}

{{ .SchemaMarkdown | trimspace }}
